import * as React from 'react'

import WhyUsSection from './WhyUsSection'

export default {
  title: 'Sections/WhyUsSection',
  component: WhyUsSection,
}

const Template = (args) => <WhyUsSection {...args} />

export const Default = Template.bind({})
Default.args = {}
